'use client'
import React from 'react';

const loading = () => {
    return (
        <h1 className="text-3xl font-bold text-black text-center mt-10">Loading...</h1>
    );
};

export default loading;